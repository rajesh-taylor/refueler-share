[package]
name = "blake3-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
blake3 = { version = "1.5", default-features = false }
wasm-bindgen = "0.2"

[profile.release]
opt-level = "z"
lto = true
