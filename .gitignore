# Environment & secrets
.env
.env.local
.env.*.local
*.pem
*.key
wrangler.toml.local

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
dist/
# …except the vendored BLAKE3 WASM bundle: it ships to refueler.io/share/ and must be
# canonical + verifiable here (Share-Sync-1). Without these, the live WASM had no source in git.
!frontend/blake3/dist/
!src/blake3/dist/
build/
.wrangler/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Test artefacts
coverage/
.dev.vars
.wrangler-test-state/
refueler-share-financial-model.xlsx

Claude outputs/
*.timestamp-*.mjs
